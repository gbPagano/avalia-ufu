#!/bin/bash
docker compose up 
# (wait $! 
# sleep 15 
# echo "Populando o banco de dados..." 
# docker exec -itd db_avalia psql -U postgres -d wikiprof -a -f /var/lib/postgresql/populate.sql && echo Banco de dados populado com sucesso.)


